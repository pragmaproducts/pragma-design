export function capitialize(string) {
    return string.charAt(0).toUpperCase() + string.slice(1);
}

export function padStart(value, padLength = 2, padString= "0") {
    return (value != null) ? value.toString().padStart(padLength, padString) : null;
}