import {ViewBase} from "../../../baremetal/lib/view-base.js";

export default class RouteParameters extends ViewBase {
    
    connectedCallback() {
     console.log(this);
       this.resourceName = this.routeParameters.resourceName;

        this.resourceId = this.routeParameters.resourceId;
    }

    routeParametersChanged(newValue, oldValue){
        console.log(newValue,oldValue);
        
    }
}