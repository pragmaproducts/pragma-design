import {ViewBase} from "./../../../baremetal/lib/view-base.js";

export default class OverflowToolbarTest extends ViewBase {
    connectedCallback() {
    }

    performToolbarAction(event) {
        console.log(event);
    }
}