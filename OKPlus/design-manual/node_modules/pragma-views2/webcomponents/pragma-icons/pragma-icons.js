class PragmaIcons extends HTMLElement {

    connectedCallback() {
        this.initTemplate();
    }

    initTemplate() {
        const test = window.templates.get("pragma-icons-template");
        const instance = document.importNode(test, true);
        this.appendChild(instance);
    }

}

customElements.define("pragma-icons", PragmaIcons);