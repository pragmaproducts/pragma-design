import {RemoteDatasource} from "./../../../lib/remote-datasource.js";

export function createDataSourceInstance() {
    const options = {
        schema: {},
        definition: {
            remote: "SomeRemote",
            action: "SomeAction"
        }
    };
    return new RemoteDatasource(options);
}