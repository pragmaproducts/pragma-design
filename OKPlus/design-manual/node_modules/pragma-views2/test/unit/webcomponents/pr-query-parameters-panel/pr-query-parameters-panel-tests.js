import {QueryParametersPanel} from "./../../../../webcomponents/pr-query-parameters-panel/pr-query-parameters-panel.js";

describe('Pragma Query Parameters Panel Tests', function() {
    const sandbox = sinon.sandbox.create();
    let instance;

    beforeEach(() => {
        instance =  new QueryParametersPanel();
    });

    afterEach(() => {
        sandbox.restore();
    });
});