import {QueryParameters} from "./../../../../webcomponents/pr-query-parameters/pr-query-parameters.js";

describe('Pragma Query Parameters Tests', function() {
    const sandbox = sinon.sandbox.create();
    let instance;

    beforeEach(() => {
        instance =  new QueryParameters();
    });

    afterEach(() => {
        sandbox.restore();
    });
});